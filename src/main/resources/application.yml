spring:
  application:
    name: stock-agent-harness

server:
  port: 8080

logging:
  level:
    root: INFO
    com.stock: DEBUG

harness:
  max-steps: 10
  scheduler:
    enabled: false
    fixed-delay-ms: 600000