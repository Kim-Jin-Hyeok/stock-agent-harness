spring:
  application:
    name: stock-agent-harness

server:
  port: 8080

logging:
  level:
    root: INFO
    com.stockagent: DEBUG

harness:
  scheduler:
    enabled: false
    fixed-delay-ms: 600000