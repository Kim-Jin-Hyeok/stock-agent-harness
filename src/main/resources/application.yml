spring:
  application:
    name: stock-agent-harness

server:
  port: 8080

logging:
  level:
    root: INFO
    com.stock: DEBUG

harness:
  max-steps: 10
  scheduler:
    enabled: false
    fixed-delay-ms: 600000

risk:
  max-order-ratio: 0.1
  max-position-ratio: 0.3